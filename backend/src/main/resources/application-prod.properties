# Production Configuration
# Override these values when deploying

# Database Configuration (Update with your production database)
spring.datasource.url=jdbc:mysql://mysql:3306/revcart_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Vanipriya@5

# MongoDB Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb://YOUR_MONGO_HOST:27017/revcart_logs}

# OAuth2 Configuration (Update redirect URI in Google Cloud Console)
spring.security.oauth2.client.registration.google.redirect-uri=${OAUTH_REDIRECT_URI:https://YOUR_DOMAIN/api/login/oauth2/code/google}

# CORS Configuration (Add your production domain)
cors.allowed-origins=${CORS_ORIGINS:https://YOUR_DOMAIN,http://YOUR_DOMAIN}

# Frontend URL (Your production frontend URL)
frontend.url=${FRONTEND_URL:https://YOUR_DOMAIN}

# Logging (Reduce logging in production)
logging.level.com.revcart=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.springframework.web=WARN
